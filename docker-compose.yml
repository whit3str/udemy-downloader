services:
  udemy-downloader:
    container_name: udemy-downloader
    image: ghcr.io/whit3str/udemy-downloader:latest
    volumes:
      - ./output:/app/out_dir:rw
      - ./keyfile.json:/app/keyfile.json:ro
    env_file:
      - .env
    command: python main.py -c $COURSE_URL
